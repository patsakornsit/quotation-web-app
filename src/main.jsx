import React from 'react';
import ReactDOM from 'react-dom/client';
import LedgerQuotationDemo from '../quotation-chat-demo.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LedgerQuotationDemo />
  </React.StrictMode>
);
